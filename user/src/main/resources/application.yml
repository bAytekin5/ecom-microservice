spring:
  application:
    name: user-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testuserservice
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create


server:
  port: 8082

